defpackage user :
   import core
   import collections

defn main () :
   val a = 1
   defn f (x) :
      match(x) :
         (x:Int) : 42
         (x:String) : "hi"
  
   defn g () -> ? :
      42 + a

   println([f, g])
   println(f(g()))

val x = [main]
x[0]()