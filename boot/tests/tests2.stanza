defpackage user :
   import core
   import collections

defn main () :
   val s = "Hello World"
   println(s[0])
   ;val table = HashTable<[Int, Int], Int>()
   ;for i in 0 to 10 do :
   ;   for j in 0 to 10 do :
   ;      table[[i,j]] = i * j
   ;val s = to-string(table)
   ;println(s)
   ;println("First char = %~" % [s[0]])
   
val x = [main]
x[0]()