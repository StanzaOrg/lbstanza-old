defpackage user :
   import core
   import collections

defn main () :
   val t = HashTable<[Int,Int], Int>()
   for i in 0 to 100 do :
      for j in 0 to 100 do :
         t[[i,j]] = i * j
   println(t)
   fatal("Print error message")

main()
