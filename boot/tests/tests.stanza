defpackage user :
   import core
   import reader   

defn test-stringstream () :
   val s = StringInputStream("Hello This is Dog")
   while not empty?(s) :
      println("%_: '%_'" % [info(s), next(s)])

test-stringstream()

;defn test-reader () :
;   val form = lex-file("compiler/stz-namemap.stanza")
;   println(form)
;   
;test-reader()