Initially, this directory uses a Makefile to execute conan to download
the asmjit library then link with stanza output.

TODO Convert this to using the new stanza proj invocation of conan

Also integrate with
  compiler/conan-package-manager.stanza
  tests/dev-package-manager.stanza

