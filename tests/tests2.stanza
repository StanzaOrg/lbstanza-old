defpackage user :
   import core
   import collections

lostanza defn get-tuple () -> ref<[Int, Int]> :
   return new Tuple{2, rand(new Int{10}), rand(new Int{10})} as ref<[Int, Int]>
   
defn main () :
   defn make-vector () :
      to-vector<[Int,Int]>(repeatedly(get-tuple, 100))
   val vs = repeatedly(make-vector)
   do(next{vs}, 0 to 10000)
   println(next(vs))

main()