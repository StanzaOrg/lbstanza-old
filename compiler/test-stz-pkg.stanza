defpackage test-stz-pkg :
  import core
  import collections
  import stz/asm-ir
  import stz/typeset
  import stz/serializer
  import stz/pkg

defn main () :
  val filename = command-line-arguments()[1]
  val pkg = load-package(filename) as StdPkg
  println("Instructions:")
  within indented() :
    println(packageio(pkg))
    do(println, asm(pkg))

main()