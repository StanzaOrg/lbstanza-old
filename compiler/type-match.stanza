defpackage stz/type-match :
  import core

;Used to represent that an overloaded match partially matches
;due to the usage of gradual expansion.
public defstruct PartialMatch <: Equalable
with: (equalable => true)
  
