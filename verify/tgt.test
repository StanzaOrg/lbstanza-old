program(main) :
   defn main (x:int) :
      locals: (y:int)
      x = add(x, x)
      y = sub(int(3), mem<hi>)
      y = x
      (x) = mem<f>(x, y)
      (x) = mem<f>(x, y) / 3
      label<f>
      goto label<f>
      branch label<f> when lt(x, y)
      return (x, y)