program(main) :
   defn main (x:int, y:int, z:int) :
      locals: ()
      branch label<a> when lt(x, x)
      h = add(x, x)
      i = add(y, y)
      goto label<end>
      label<a>
      w = add(z, z)
      (z) = mem<f>(x)
      w = add(y, y)
      label<end>
      return (z)
      
