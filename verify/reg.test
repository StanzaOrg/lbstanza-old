;unit :
;   def x:int
;   block b1 :
;      (x) = Args
;      goto ()

;unit :
;   def x:int
;   block b1 :      
;      (x,y,z) = op()
;      goto (b2, b3)
;   block b2 :
;      () = op(x)
;      () = op(y)
;      goto (b4)
;   block b3 :
;      (y) = op()
;      (z) = f(x)
;      () = op(y)
;      goto (b4)
;   block b4 :
;      () = op(z)
;      goto ()

unit :
   def x:int
   block b1 :
      (z) = op()
      (x0) = op()
      (x1) = op()
      (x2) = op()
      (x3) = op()
      (x4) = op()
      (x5) = op()
      (x6) = op()
      (x7) = op()
      (x8) = op()
      (x9) = op()
      (x10) = op()
      (x11) = op()
      (x12) = op()
      (x13) = op()
      (x14) = op()
      (x15) = op()
      (x16) = op()
      (x17) = op()
      (x18) = op()
      (x19) = op()
      (x20) = op()      
      goto (b2, b3)
   block b2 :
      () = op(x0)
      () = op(x1)
      goto (b4)
   block b3 :
      () = op(x2)
      () = op(x3)
      goto (b4)
   block b4 :
      (x0) = op(x0)
      () = op(x1)
      () = op(x2)
      () = op(x3)
      () = op(x4)
      () = op(x5)
      () = op(x6)
      () = op(x7)
      () = op(x8)
      () = op(x9)
      () = op(x10)
      () = op(x11)
      () = op(x12)
      () = op(x13)
      () = op(x14)
      () = op(x15)
      () = op(x16)
      () = op(x17)
      () = op(x18)
      () = op(x19)
      () = op(x20)
      goto ()

;test :
;   block start :
;      goto (b1, b2)
;   block b1 :
;      (a) = op()
;      goto (b3)
;   block b2 :
;      (a) = op()
;      goto (b3, b4)
;   block b3 :
;      (b) = op(a)
;      goto (b5)
;   block b4 :
;      (c) = op(a)
;      goto (b5)
;   block b5 :
;      () = f(a)
;      () = op(a)
;      goto ()
;
;test :
;   block start :
;      (a) = op()
;      () = f()
;      (b) = op(a)
;      () = f()
;      (c) = op(b)
;      () = f()
;      () = op(c)      
;      goto ()
;
;test :
;   block b1 :
;      (x, y, z) = f()
;      goto (b2)
;   block b2 :
;      () = op(x, y)
;      goto ()
;
;test :
;   block myblock :
;      (x y z) = args
;      (x) = op(y)
;      (y) = op(z)
;      goto (b1, b2)
;   block b1 :
;      (a) = op()
;      (x) = op(x)
;      (x) = op(y)
;      (z, h) = f(x, x)
;      () = op(y, y)
;      (h) = op(x)
;      goto (b3)
;   block b2 :
;      (h) = op(y)
;      goto (b3)
;   block b3 :
;      () = op(z, h)
;      goto ()
;
;test :
;   block myblock :
;      (x y z) = args
;      (x) = op(y)
;      (y) = op(z)
;      goto (b1, b2)
;   block b1 :
;      (a) = op()
;      (x) = op(x)
;      (x) = op(y)
;      (z) = f(x, x)
;      () = op(y, y)
;      () = f()
;      (h) = op(x)
;      () = op(z)
;      goto (b3)
;   block b2 :
;      (h) = op(y)
;      goto (b3)
;   block b3 :
;      () = op(z, h)
;      goto ()
;
;test :
;   block b1 :
;      (a, b, c, d, e, f, g, h, i, j, k) = args
;      goto (b2)
;   block b2 :
;      () = op(a)
;      () = op(b)
;      () = op(c)
;      () = op(d)
;      () = op(e)
;      () = op(f)
;      () = op(g)
;      () = op(h)
;      goto (b3)
;   block b3 :
;      () = op(k)
;      goto ()
;
;test :
;   block b1 :
;      (a) = op()
;      (b) = op(a, a)
;      (c) = op(b, b)
;      (d) = op(c, c)
;      (e) = op(d, d)
;      (f) = op(e, e)
;      (g) = op(f, f)
;      (h) = op(g, g)
;      (i) = op(h, h)
;      (j) = op(i, i)
;      (k) = op(j, j)
;      (l) = op(k, k)
;      (m) = op(l, l)
;      (n) = op(m, m)
;      (p) = op(n, n)
;      goto ()
;
;test :
;   block b1 :
;      (a) = op()
;      (b) = op(a, a)
;      (c) = op(b, b)
;      (d) = op(c, c)
;      (e) = op(d, c)
;      (f) = op(d, e)
;      (g) = op(f, c)
;      (h) = op(a, g)
;      (i) = op(h, g)
;      (j) = op(f, i)
;      (k) = op(j, e)
;      (l) = op(d, k)
;      (m) = op(l, c)
;      (n) = op(m, b)
;      (p) = op(a, n)
;      () = f(a, b, c, d, e, f, g, h, i, j, k, l, m, n, p)
;      goto ()