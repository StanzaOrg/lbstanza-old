($begin

($package core)
($public ($begin
   ($deftype Int () ($none))
   ($deftype String () ($none))
   ($deftype Tuple (t) ($none))
   ($deftype List (T) ($none))
   ($deftype Array (T) ($none))))

($defvar z String ($none))

($do
   ($fn (($none)) ($none) (x) ($begin
      ($do x 3)
      ($do x z)))

   ($fn (($none)) ($none) (x)
      x))







)