($begin

($package core)
($public ($begin
   ($deftype Int () ($none))
   ($deftype String () ($none))
   ($deftype Tuple (t) ($none))
   ($deftype List (T) ($none))
   ($deftype Array (T) ($none))))

($defmulti map (($cap T) ($cap S)) (($-> (T) ($cap S)), ($of List ($cap T))) ($of List S))
($defmulti map (($cap T) ($cap S)) (($-> (T) ($cap S)), ($of Array ($cap T))) ($of Array S))

($defvar h ($or ($of List Int) ($of Array String)) ($none))

($def zs2 ($none) ($do map
   ($fn (($or Int String)) ($none)
      (x)
      x)
   h))







)