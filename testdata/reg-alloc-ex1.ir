program :
  num-vars: 10
  label L0
  (V0, V1) = op0()
  op0(V1)
  branch (L1, L2)
  label L1
  (V2) = op2()
  (V3) = op2()
  (V1) = op0(V2, V3)
  (V2) = op1(V3)
  goto L3
  label L2
  (V1) = op0(V0, V1)
  (V2) = op0(V1, V0)
  () = op4(V2, V0)
  (V1) = op0(V0, V1)
  goto L3
  label L3
  op3(V0, V1, V2)

model (V0) = op0(V1:Rx, V2:Rx) (temp V3 V4 V5) :
  release V1
  assign Rx to V0

model () = op1() : ()

model () = op2() : ()

model () = op3(V0:Rx, V1:Rx, V2:Rx) :
  return

model () = op4(V0:Rx, V1:Rx) :
  clear-registers