program :
  num-vars: 10
  label L0
  (V0, V1) = op0()
  op0(V1)
  branch (L1, L2)
  label L1
  (V2) = op2()
  (V3) = op2()
  (V1) = op0(V2, V3)
  (V2) = op1(V3)
  goto L3
  label L2
  (V1) = op0(V0, V1)
  (V1) = op0(V0, V1)
  goto L3
  label L3
  op3(V0, V1, V2)

model (V0) = op0(V1, V2) (temp V3 V4 V5) :
  require Rx for V2
  require Rx for V1
  release V1
  require Rx for V0

model () = op1() : ()

model () = op2() : ()

model () = op3(V0, V1, V2) :
  return